# Commands
```
cargo run create-market --keypair pool_market_keypair.json
cargo run create-pool --market <PUBKEY> --token <PUBKEY>
cargo run create-pool-borrow-authority --borrower <PUBKEY> --pool <PUBKEY>
```
