# Commands
```
cargo run create-market --keypair income_pool_market_keypair.json --general-market <PUBKEY>
spl-token create-token token_keypair.json
cargo run create-pool --market <PUBKEY> --token <PUBKEY>
```
